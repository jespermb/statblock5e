html
    head
        title=title
        link(rel='stylesheet', href='/css/style.css')
        meta(name='viewport' content='windth=device-width, initial-scale=1')
    body
        header
            h1 Creature template
        section
            nav
                ul
                    for item in menu
                        li
                            a(href = item.path)= item.name
            content
                p Select a link in the sidebar.
        footer
            h3 Footer
